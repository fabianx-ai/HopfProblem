# Round 7 checklist packet 06

Base: the merged head after the preamble and names branches (see the task prompt). Rubric: `Lib/reports/textbook-audit/RUBRIC.md`; per-file findings: `grep -A14 '^## <path>' Lib/reports/textbook-audit/group-*.log`.

| file | audit | work | twin (from the audit) | suggestion (from the audit) |
|---|---|---|---|---|
| `Lib/Geometry/Manifold/Whitney/EmbeddedArcs.lean` (3300 l.) | C | docstrings 0/88 | Milnor, Lectures on the h-cobordism theorem §6 (Whitney lemma preparation, embedded arcs joining intersection points; na | move the three `finrank ℝ E = 6` belt statements to `Hopf/Proof`, and regroup the rest into `Whitney/Arcs.lean` (embedded arcs avoiding finite sets, documented) |
| `Lib/Geometry/Manifold/Whitney/FrameField.lean` (2549 l.) | C | docstrings 0/121 | Milnor, Lectures on the h-cobordism theorem §§5–6 (Whitney trick, named in docstring); no Mathlib twin (genuine gap for  | split the general frame-field/complement lemmas (l.179–1268) into a documented `Whitney/FrameField.lean` and move the rank-three and `MorseSurgeryData` material |
| `Lib/Geometry/Manifold/Whitney/RankThreeModel.lean` (2861 l.) | C | docstrings 0/160 | Milnor, Lectures on the h-cobordism theorem §6 (Whitney's lemma / cancellation of intersection points); no Mathlib twin  | keep the planar `WhitneyPairModel.GraphMotion` section as a documented library file and move the `RankThree*` charts and cancellation (l.427–2860) to the projec |
| `Lib/GroupTheory/Abelianization/SemidirectProduct.lean` (216 l.) | A | `: Type` binders ×3 (no `Type*`) | `H₁(K ⋊ Q) ≅ H₁(Q) ⊕ H₁(K)_Q` (standard; e.g. Brown, Cohomology of Groups, Ch. II Ex./LHS in low degree); no Mathlib twi | add `## References` with the textbook citation for the `H₁` formula and propose upstream as is |
| `Lib/GroupTheory/GroupExtension/Abelianization.lean` (46 l.) | A | `: Type` binders ×3 (no `Type*`) | corollary of `H₁(K ⋊ Q) ≅ H₁(Q) ⊕ H₁(K)_Q` via Mathlib/GroupTheory/GroupExtension/Basic.lean `GroupExtension.Splitting.s | merge into `Abelianization/SemidirectProduct.lean` as a final section (one declaration does not need its own file) or leave as is |
| `Lib/LinearAlgebra/ColumnKernel.lean` (80 l.) | C | manuscript citation only | the kernel of `LinearMap.coprod f e` with `e` an isomorphism is the graph of `-e⁻¹ ∘ f`; nearest Mathlib: Mathlib/Algebr | restate as `LinearMap.kerCoprodEquivOfRight (f : A →ₗ[R] D) (e : B ≃ₗ[R] D) : ker (f.coprod e) ≃ₗ[R] A` over any `[Semiring R]`, dropping the spurious instance  |
| `Lib/LinearAlgebra/ExteriorPower/ExteriorProductCoordinates.lean` (116 l.) | B | `: Type` binders ×2 (no `Type*`) | Bourbaki, Algebra III §8.5 (multiplication of exterior powers on a basis, `e_S ∧ e_T = ±e_{S∪T}`); nearest Mathlib: Math | add a `## References` (Bourbaki A III §8.5) and a one-line description of the sign convention to the module docstring |
| `Lib/LinearAlgebra/ExteriorPower/MatrixCoordinates.lean` (99 l.) | B | `: Type` binders ×1 (no `Type*`) | Bourbaki, Algebra III §8.5–8.6 (the matrix of `⋀ⁿ A` is the matrix of `n×n` minors; compound matrix); Mathlib/LinearAlge | replace the provenance paragraph with `## Main definitions` and a Bourbaki reference, and consider naming `finMatrix` as the compound matrix (`Matrix.compound n |
| `Lib/LinearAlgebra/ExteriorPower/MinorCoordinates.lean` (198 l.) | C | manuscript citation only | Cauchy–Binet for compound matrices (Bourbaki A III §8.6; Horn–Johnson §0.8.7); sibling `Lib/LinearAlgebra/ExteriorPower/ | delete the `ℤ` duplicates in favour of `exteriorPower.finBasis`/`finMatrix`, move the lex enumeration to an order file, and keep only `cauchyBinet_minors` here, |
| `Lib/LinearAlgebra/ExteriorPower/ReindexedCoordinates.lean` (117 l.) | B | `: Type` binders ×14 (no `Type*`) | `Module.Basis.reindex` transport of the compound-matrix formula; Mathlib/LinearAlgebra/Basis/Basic.lean (`Basis.reindex` | define `reindexedFinMatrix` as `Matrix.reindex` of `finMatrix` (or drop the file and use `Basis.reindex` at the use site) and add a `## Main definitions` list |
| `Lib/LinearAlgebra/Matrix/TransvectionReduction.lean` (217 l.) | C | docstrings 1/10 | Milnor, Lectures on the h-cobordism theorem §7 (algebra of Thm 7.6: a unimodular row is column-reducible to one containi | rename to `Matrix.*`, generalise `Type` to `Type*`, document each lemma, and re-derive the row reduction from Mathlib's `Matrix.Pivot`/Smith-form API where poss |
| `Lib/RepresentationTheory/FreeGroupCoinvariants.lean` (68 l.) | B | `: Type` binders ×3 (no `Type*`) | `M_F = M / ⟨(x_a − 1)m⟩` for a free group `F = F(A)` (Brown, Cohomology of Groups II.3 / coinvariants presented by gener | state the general `Coinvariants.ker_eq_span_of_closure_eq_top` for a generating set and derive the free-group case; delete the disclaimer sentence |
| `Lib/RepresentationTheory/FreeGroupGeneratorCokernel.lean` (136 l.) | B | `: Type` binders ×5 (no `Type*`) | `H₀(F(A); M) ≅ M_F ≅ coker(⊕ₐ M → M, (mₐ) ↦ Σ (xₐ − 1)mₐ)` (Brown, Cohomology of Groups II.3, Ex.; the start of the free | state the unoriented map `freeGroupGeneratorDifference` as the primary object with the standard `H₀`/coinvariants identification, and obtain the oriented varian |
| `Lib/Topology/Covering/FiberFrameCentralizer.lean` (67 l.) | B | `: Type` binders ×3 (no `Type*`) | deck transformations of a normal cover commute with the monodromy of a loop fixing the frame (Hatcher §1.3, Prop. 1.39 v | rename the file to `Covering/MonodromyCommute.lean`, drop "frame" from the prose, and add a Hatcher reference |
| `Lib/Topology/Covering/QuotientConnectedness.lean` (63 l.) | A | `: Type` binders ×3 (no `Type*`) | Hatcher §1.3 (a normal cover with transitive monodromy on the fibre is connected; converse of `IsQuotientCoveringMap.fun | propose upstream into Mathlib's `Topology/Covering/Quotient.lean` next to `fundamentalGroupToMulOpposite_surjective`, adding a Hatcher reference |
