# Review of Lib/reports/round-7/packets/RECEIPT-04.md

**Verdict: ACCEPT WITH FINDINGS** — every structural claim (18 files, one commit each, 335 docstrings, 26 pins lifted, 13 binders widened, no file outside the packet, no sorry/axiom, forced pins genuinely forced at the base) checks out against the branch; the findings are one docstring that states a stronger hypothesis than the theorem has, small arithmetic/unit slips in the receipt, six unmentioned `: Type` binders, a missing name for the +1 auxiliary constant, and loose citations.

## Findings

1. `[docstring]` `MorseCancellation.exists_clean_two_sheet_arc` (`Lib/Geometry/Manifold/Immersion/Relative.lean:3930-3935` at `d950428a^2`, commit `7dd9dcbd`) and the module docstring's `## Main results` bullet for it say **"Two disjoint embedded immersed surfaces in a five-manifold"**. The statement has no disjointness hypothesis: it takes `(hfe : IsEmbedding f) (hge : IsEmbedding g)`, `(hdim : finrank ℝ E = 5)`, `(hx : f x ∉ Set.range g) (hy : g y ∉ Set.range f)` and a path `γ : Path (f x) (g y)`; `range f` and `range g` may meet elsewhere. The docstring overstates the hypothesis (the theorem is stronger than described). Fix: "two embedded immersed surfaces, with `f x` off the second and `g y` off the first".

2. `[incomplete]` `Lib/Geometry/Manifold/Morse/AdaptedWindows.lean` (commit `e5d2b172`) contains six declarations with un-widened `: Type` binders — `exists_relative_level_surgery_system {E M : Type}`, `reaches_lower_of_excluded_critical_limit`, `reaches_old_lower_of_belt_avoidance`, `not_backward_basin_on_upper_level`, `transported_backward_basin_image {E M X : Type}`, `reaches_lower_in_regular_band` (lines 389/456/483/504/521/564 at head `39f1d12b`, still `Type`). The receipt says only "no universe pins or `: Type` binders were counted for this file" (true of the audit table) but item 4 of the packet is exactly binder widening, and for `Relative.lean` the receipt did say "none found". These should have been listed as found-and-left (with a reason) or widened. Neither round 8 nor anything later touched them.

3. `[wrong receipt]` (arithmetic/units) The summary row "`.{0}` pins left, forced … 81" does not match the per-file entries, which sum to 18+20+6+8+12+2+1+1+1+9+1+1 = **80**; the audit's per-file totals (106) minus the 26 lifted also give 80. Units are mixed: `ResolutionTransgression.lean` "9 left" — the file has 7 textual `.{0}` occurrences at the branch head (`git show d950428a^2:… | grep -c '\.{0}'`), 12 before; the "3 generalised" there are declarations, while `StalkLocalCriterion.lean` "all 3" are textual pins on one `variable` line (one declaration). "26 generalised" is therefore 12+8+3(decl)+3(textual). None of this affects soundness.

4. `[incomplete]` The receipt reports "constants before 21719 after 21720" and "lost 249 added 250 … all auxiliary" but never names the one net-new constant. The packet's own `envdiff.json` is not in the repo (only `Lib/reports/round-7/envdiff-merged-d950428a.{json,txt}` exists), so this cannot be re-run. From the merged envdiff the only added-not-lost names are `CategoryTheory.Sheaf.Leray.higherDirectImageResolutionSheafificationIso._proof_5` (a `HigherDirectImageSheafification.lean` declaration, commit `cff67f3d` — this packet's) and `TopCat.SingularCochainSheaf.unit._proof_1` (another packet). The receipt should name it and say why a new proof-term auxiliary appears (the universe-generalised `sheafification`/`homologyPresheaf` now need a universe-unification obligation discharged in that def). At head `39f1d12b` the constant no longer exists (round 8 `a140e4b1` re-lifted the file), so I could not `#print` it.

5. `[incomplete]` The receipt lists three outside sources of forced pins. A fourth is needed for `sheafCohomologyAddCommGroup {X : TopCat.{0}}` (`ResolutionTransgression.lean:67`): it wraps `CategoryTheory.Sheaf.cohomologyAddCommGroup`, which at the base is `instance cohomologyAddCommGroup {X : TopCat.{0}}` (`Lib/Topology/Sheaves/Cohomology/AddCommGroup.lean:30` at `9552305f`). The pin is genuinely forced; the receipt just doesn't say by what.

6. `[citation]` Loose or unverifiable references, all low severity:
   * `StalkLocalCriterion.lean` module docstring cites **Godement II.1.1** for "the filtered-colimit criterion for bijectivity of a map out of a stalk". Godement II §1.1 is the definition of (pre)sheaves and germs; there is no such criterion stated there. The Mathlib names it also cites (`TopCat.Presheaf.exists_germ_eq`, `germ_res_apply`) exist (`Mathlib/Topology/Sheaves/Stalks.lean:433,121`).
   * `Relative.lean`: **Hirsch Ch. 8** for "relative immersion and embedding theorems" (module docstring; on `exists_immersion_on_compact_rel`, `exists_relative_compact_embedding`, `exists_relative_compact_curve_embedding`, `exists_clean_two_sheet_arc`). Hirsch Ch. 8 is *Isotopy*; the immersion/embedding theorems are Ch. 2 §2 (Whitney) and Ch. 3 §2 (general position). "Ch. 8" was inherited from the pre-packet docstring, but the packet re-asserted it on four declarations. Ch. 3 (general position) and Ch. 4 §5 (tubular neighbourhoods), which the packet added, are right.
   * `ResolutionPostnikovD2.lean`: **Verdier II.4.3** and **Kashiwara–Schapira §12–13** for "`d₂` of a Postnikov tower is the connecting map of the two-slice triangle" — I could not confirm the section numbers; KS's t-structure/Postnikov material is Ch. 10, spectral sequences of filtered objects are Ch. 12. Not sure.
   * `Neeman §1.1` for the shifted-triangle sign: plausible (the sign is in the definition of pre-triangulated categories, Neeman Ch. 1 §1.1), not verified to the remark number.
   Verified as stated: Hartshorne III.8.1 (Rⁱf_*F = sheafification of V ↦ Hⁱ(f⁻¹V,F)), Hartshorne II.1.2 (associated sheaf, stalk iso), Hartshorne III.8 (Rⁱf_* as right derived functors), Godement II.4.17.1 (Leray spectral sequence), Weibel 5.8.6 (Leray SS as Grothendieck SS of `Γ∘f_*`), Weibel 2.3.10 (right adjoint of an exact functor preserves injectives), Milnor h-cobordism Thm 4.1 (rearrangement) / Thm 4.8 (self-indexing) / §6 (middle-dimension cancellation, Whitney trick), Hirsch Ch. 3 / Ch. 4 §5, Mathlib `Pretriangulated.preadditiveCoyoneda_homologySequenceδ_apply` (`Mathlib/CategoryTheory/Triangulated/Yoneda.lean:68`, `#check` passed), `HasDerivedCategory.standard : HasDerivedCategory.{max u v} C` (so `ULift.{1}` on `AbelianSheaf Y : Type 1` is indeed forced), `Circle.exp`, `Function.Periodic`.

7. `[nit]` `sheafification_preservesFiniteColimits` docstring cites "Godement II.1.2; Hartshorne II.1.2" for *exactness* of sheafification; both places construct the associated sheaf and prove the stalk isomorphism, exactness being a corollary. Acceptable but imprecise.

No `[unsound]` finding. Deletions: none (verified, `git diff --stat` shows only additions/edits within the 18 files; no declaration removed — checked by comparing `^(theorem|def|lemma|instance|abbrev)` heads before/after per file). Renames: none.

## Claims checked

| claim | status | how |
|---|---|---|
| 18 files, each built and committed alone; commits in the listed order with the listed hashes | verified | `git log 9552305f..d950428a^2`: 18 file commits + receipt commit `378f5627`; hashes/order match the receipt table; `git diff --stat` = 18 `.lean` + receipt |
| Merge `d950428a` carries exactly the branch | verified | `diff <(git diff 9552305f d950428a^2) <(git diff d950428a^1 d950428a)` (index lines stripped) empty |
| "No file outside the packet was edited" | verified | diff stat |
| Commit messages describe the commit; trailers present | verified | all 19 commits carry `Co-Authored-By` + `Claude-Session` |
| 335 declaration docstrings added (2+0+3+0+7+0+0+0+0+0+0+1+0+0+214+17+56+35) | verified | `grep -c '^/--'` before/after per file: exactly those deltas (Relative 0→214, AdaptedWindows 0→17, CircleGluing 0→56, RearrangementAmbient 0→35, OpenRestrictionComposition 10→12, HDIS 16→19, RCP 14→21, SSC 6→7, others unchanged) |
| 18 module docstrings rewritten with a textbook reference | verified | read all 18 diffs |
| 5 declaration docstrings de-processed (4 in `Stalk.lean`, `pushedResolutionCohomologyPresheafIsoPositive`) | verified | diffs of `9e06b8e0`, `3ba7c621` |
| 26 pins lifted: HDIS 12, RCP 8, RT 3, SLC 3 | verified (units mixed, finding 3) | textual `.{0}` counts 18→6, 20→12, 12→7, 3→0; declarations named in the receipt are the ones moved into `section Generic` / `Evaluation` / `PresheafLevel` / `universe u` |
| 81 pins left, forced | partly | sum is 80 (finding 3); "forced" verified below |
| `pushforwardAdditive` is `variable {X Y : TopCat.{0}}` at the base | verified | `git show 9552305f:Lib/Topology/Sheaves/AddCommGrpPushforward.lean` line 26 |
| `ConstantSheaf.integralSheaf (X : TopCat.{0})` at the base | verified | `GlobalSections.lean:34` at `9552305f` (lifted to `.{u}` only by round 8 `e8161fd1`) |
| `OpenRestriction`, `OpenRestriction/Cohomology`, `FiniteClosedPushforward/Cohomology` pinned at base | verified | `variable {X : TopCat.{0}}` / `{X Y : TopCat.{0}}` lines 32/36/34 at `9552305f`; all `.{u}` at head after round 8 |
| `ULift.{1}` in `resolutionPostnikovE₂Iso` forced by `HasDerivedCategory.standard` | verified | `#check @HasDerivedCategory.standard : … HasDerivedCategory.{max u v}`; `AbelianSheaf Y : Type 1`, hom `Type 0`, so the derived Hom lives in `Type 1` while `E₂` is `Sheaf.H.{0}` |
| Universe lifts keep the `u = 0` statement and add no hypothesis | verified (sample) | `AbelianSheaf`, `abelianSheafHasExt` (`HasExt.{0} (AbelianSheaf X)` → `HasExt.{u} (AbelianSheaf.{u} X)`), `pushforward`, `sheafification*`, `presheafStalk_*`, `stalkHomologyPresheafIso` (only explicit `AddCommGrpCat.{u}` added), `presheafEvaluation*`, `presheafPushforward*`, `stalkMap_bijective_of_local_lift_kill` — diffs are `.{0}`→`.{u}` only; `#check` of the last at head shows the same hypotheses |
| `E₂`: only the `: Type` ascription dropped | verified | diff; `#check @Sheaf.Leray.E₂ : … → Type u_1` at head (head already re-lifted by round 8) |
| 13 binders widened (12 in `RearrangementAmbient`, 1 in `CircleGluing`), `sheetSum : Type u → ℕ → Type u` | verified | diff `166b73ce` (21 textual `: Type` → 0, across the 12 named declarations), `aa15cc58`; `#check @MorseRearrangement.sheetSum : Type u_1 → ℕ → Type u_1` |
| `CoyonedaTriangleShift` binder "not a defect" (`universe v u`, `{C : Type u} [Category.{v} C]`) | verified | file lines 39, 41 |
| Envdiff: 0 source lost/added; 129 changed types all explained; the 91-name list | verified against the merged envdiff | all 91 listed names plus the 38 named ones are in `changed_type_all` of `envdiff-merged-d950428a.json`, all classified PROOF-NAMING, `changed_type_source = 0`; per-packet envdiff itself not in repo (finding 4) |
| "0 source declarations lost, 0 added"; net +1 constant | verified / partly | merged `lost - added` sets: nothing lost-not-added; added-not-lost = two `_proof_n` auxiliaries, one from this packet (finding 4) |
| No `sorry`/`axiom`/`admit`, no `set_option`, no `@[simp]`/`noncomputable`/`private`/`unsafe`/`native_decide` changes, no `import Hopf` in `Lib` | verified | grep over `+`/`-` lines of the branch diff (only prose hits); `grep -rn "import Hopf" Lib/` hits only `Lib/docs/*.md` |
| `Lib.AxiomAudit` axiom sets | not re-run | one spot check: `stalkMap_bijective_of_local_lift_kill` depends on `[propext, Classical.choice, Quot.sound]` |
| Docstrings: index / codimension / homeomorphism side (Morse) | verified, 31 sampled | see below |

Docstrings checked against the full statement (31): AdaptedWindows — `exists_ordered_index_cut` (index ≤ k below `a`, ≥ k+1 above; `f q < a`), `no_connection_of_upper_index_zero` (`finrank NegativeCoordinates q = 0`), `no_connection_of_lower_positive_zero` (`finrank PositiveCoordinates p = 0` = index `dim M`), `attachingSphere_reaches_lower_cut`, `exists_same_flow_windows_avoiding_level`, `transported_attaching_range_iff` (backward orbit → attaching/descending sphere), `not_backward_basin_on_upper_level`, `reaches_lower_of_excluded_critical_limit`, `exists_relative_level_surgery_system`, `reaches_lower_in_regular_band`, `exists_relative_regular_level_isotopy_realization`. RearrangementAmbient — `exists_relative_ambient_patch_step` (`=`), `exists_supported_ambient_transverse_in_open` (`=`), `exists_supported_ambient_disjoint_in_open` (`<`), `exists_whole_family_avoidance`, `upperValueRank_consecutive`, `finiteIndexDisorder_swap_lt`, `beforeValueRank_exchange_lt`, `exists_adjacent_index_inversion`. CircleGluing — `transported_attaching_basin_iff` (`e : UpperLevel p ≃ₜ LowerLevel q`, backward orbit → `q` ↔ transported attaching sphere of `q`: right side), `dense_regular_level_minimum_basins`, `exists_unique_connection_of_unit_level_count`, `bijective_beltNormal_comp_of_transverse` (`m`-sphere, belt sphere `n`-sphere, `n+1 = finrank Positive`), `exists_embedded_circle_through_arc` (`dim ≥ 3`), `exists_relative_embedded_avoidance_in_open_of_isClosed_range` (`dim E = 2`, `dim N ≥ 5`, `dim E + dim E' < dim N`). Relative — `exists_smooth_path_avoiding_finite` / `exists_pointMoving_fixing_finite` (`2 ≤ dim`), `exists_small_collision_removing_parameter` (`2 dim E < dim F`), `exists_small_embedding_avoiding_parameter`, `exists_embedded_avoidance_on_compact_of_isClosed_image_controlled`, `dimH_bad_parameters_le` (`3 + dim F`), `dimH_collision_parameters_le` (`4 + dim F`), `dense_injective_immersive_parameters` (`5 ≤ dim F`), `exists_isotopic_two_points_in_dense`, `smoothTime` (`projIcc 0 1 ∘ smoothTransition`), `exists_immersion_on_compact_rel` (`5 ≤`), `exists_relative_compact_embedding_twoDimensional` (`dim E = 2`, `5 ≤`), `exists_relative_compact_curve_embedding` (`3 ≤`), `exists_small_affine_immersion` (`3 ≤`), `exists_compact_embedding_of_immersion_within_target` (`2 dim E < dim G`), `exists_clean_two_sheet_arc` (finding 1). All others match. Leray — `higherDirectImageResolutionSheafificationIso`, `restrictionPushforwardIso_hom_app`, `stalkSheafificationUnitNatTrans` (direction `P_y ⟶ (P⁺)_y`), `derivedStalkIso`, `derivedStalkEvaluation_germ`, `pushforwardPreservesInjectiveObjects`, `resolutionPostnikovE₂_d₂_eq_transgression` (`(0,q+1) → (2,q)`) match. A further ~120 docstrings in the three Morse/Immersion files were read against the declaration head only (binders, not conclusion).

## Not checked

* The packet's own `envdiff.json`/dump files (not in the repo); the receipt's envdiff numbers (249/250/129) are taken on trust, cross-checked only through the merged envdiff.
* `lake build` outputs and the full `Lib.AxiomAudit` axiom-set claim (no build permitted; one `#print axioms` spot check).
* The "experiment" error message (`Functor.Additive.{0,1,0,1}` vs `{u,u+1,u,u+1}`) was not reproduced; the underlying pin (`pushforwardAdditive` at `TopCat.{0}`) was verified at the base, which is what makes the failure inevitable.
* ~180 of the 335 docstrings (mostly short lemma docstrings in `Relative.lean` and `CircleGluing.lean`) were read only against the declaration head.
* Section numbers for Godement II.4.11, Verdier II.4.3, Kashiwara–Schapira §12–13, Neeman §1.1, Whitney 1936 Thm 5 (no copies available).
* Whether the six `: Type` binders of finding 2 can be widened (would need elaboration; they are pinned neither by the receipt's reasons nor obviously by their dependencies).

## Tool notes

* The receipt's envdiff run is unreproducible: the per-packet `envdiff.json` it names is not committed, and the merged envdiff classifies all 1438 changed types as PROOF-NAMING with no per-packet attribution. Committing the per-packet JSON (or at least the `added − lost` and `lost − added` name sets) beside each receipt would have made finding 4 a one-line check.
* Receipts should count universe pins in one unit (declarations or textual occurrences) and make the summary table a computed sum of the per-file rows; the 80/81 and 7/9 slips are the kind a `grep -c` in the receipt would prevent.
* For `: Type` binder work, a receipt line per file of the form "found N, widened M, left K because …" (as `Relative.lean` had) would have removed finding 2.
* The head has since moved (round 8 re-lifted the whole Leray cluster and split/renamed the Morse files), so any check of this packet's after-state has to be done with `git show d950428a^2:` rather than at the built head; a per-round built snapshot would have let the `_proof_5` constant be inspected.
