# Fix receipt: `r7-packet-09` (agent p09, 2026-09-21)

Branch `fix/p09`, base `62d45257` (`lib/integration`). Docstring and citation edits only: no
statement, name, universe, import or proof was touched, so no environment dump was taken (rules §5,
"docstring-only agents skip the dump"). Assigned findings: 1, 2, 3, 4, 5, 8, 9, 10, 11.

## Per finding

**Finding 1 — done** (`03a708e3`). `Lib/Topology/Sheaves/OpenEmbeddingCohomology.lean`.
`restriction_rightAdjoint` is now documented as "a right adjoint, namely of extension by zero `j_!`"
and `restriction_leftAdjoint` as "also a left adjoint, namely of the pushforward `j_*`", the wording
of the correct pair in `OpenRestriction.lean:165,170`. Checked against the witnesses:
`sheafPullbackConstruction.sheafAdjunctionContinuous (openImage f hf)` is `j_! ⊣ j^*`, and
`(openImage f hf).sheafAdjunctionCocontinuous` is `j^* ⊣ j_*`.

**Finding 2 — done** (`a0193456`). `FiniteClosedPushforward/Exact.lean`. "Hartshorne III Prop. 2.4
and its proof" (module docstring) and "Hartshorne III Prop. 2.4" (`pushforward_preservesInjectiveObjects`)
are replaced by the argument in words — `f^{-1}` preserves finite limits, hence monomorphisms, and
the right adjoint of a functor preserving monomorphisms preserves injective objects — with
"cf. Hartshorne III Lemma 6.1". That is exactly the file's proof: `pullback_preservesFiniteLimits`,
then `preservesMonomorphisms_of_preservesLimitsOfShape`, then
`Functor.preservesInjectiveObjects_of_adjunction_of_preservesMonomorphisms` applied to
`pullbackPushforwardAdjunction`.

**Finding 3 — done** (`a0193456`, `6e1dd154`). `Cohomology.lean` module docstring "III Ex. 4.1 and
III Ex. 8.2" and the two declaration docstrings (`cohomologyForward_bijective`, `cohomologyEquiv`)
"III Ex. 8.2", and `Composition.lean` "III Ex. 8.2", now read "III Ex. 8.1 (Leray)". In
`Exact.lean` the module docstring's "II Ex. 1.19 and III Ex. 8.2" becomes "cf. … II Ex. 1.19, which
is the special case of a closed embedding, where the fibres have at most one point" (III Ex. 8.2 is
dropped there: that file is about exactness, not cohomology), and `pushforward_exact`'s citation
becomes "cf. Hartshorne II Ex. 1.19 for the closed-embedding case".

**Finding 4 — done, as section references** (`be89316b`, `abb32368`, `41a8aa9a`). I could not verify
either Bredon item number, so no number is asserted: "Bredon III Prop. 1.1" (`GlobalPatch.lean`,
`GlobalPatchLocal.lean`) and "Bredon III Thm. 1.1" (`ComparisonH1.lean` ×2, `ComparisonPositive.lean`
×2) are all now "Bredon III §1", which is the section both families meant and which
`ComparisonPositive.lean:40` and `DegreeZeroAcyclic.lean:50` already used. Likewise I could not
decide between Godement II.4.3 and II.3.1 for "flasque ⇒ acyclic", so both become "Godement II §§3–4"
(`H1Vanishing/Flasque.lean`, `DegreeZeroAcyclic.lean` ×2). Not touched: "Godement II.3.9" in
`ComparisonPositive.lean:20` (a different statement, not flagged by the review) — it is an
unverified item number and a candidate for the same treatment in a later pass.

**Finding 5 — done** (`ed6f57fa`). `OpenRestriction/StalkCriterion.lean:17`: "Kashiwara–Schapira,
*Sheaves on Manifolds*, Prop. 2.3.6" → "cf. Kashiwara–Schapira, *Sheaves on Manifolds*, §2.3".

**Finding 8 — done** (`abb32368`). `SingularCochainSheaf/ComparisonH1.lean`, `h1Comparison`. The
declaration takes `(hLC : LocallyContractibleSpace X)` and
`[IsIso (HomologicalComplex.homologyMap (globalCochainComparison X A) 1)]` and produces
`AddCommGrpCat.of (Sheaf.H.{0} (TopCat.ConstantSheaf.sheaf X A) 1) ≅
(AlgebraicTopology.SingularCochains.complex X A).homology 1`. The docstring now lists the instance
hypothesis and says that its invertibility is the content of the comparison theorem and is assumed
here, not proved.

**Finding 9 — done** (`6b5b74f3`). `SheafificationLocalGerm.lean` module docstring. The two
ingredients are now separated: `TopCat.Presheaf.stalkFunctor_map_germ` (the stalk map of a presheaf
morphism sends a germ to the germ of the image section — what the proof uses, as
`stalkFunctor_map_germ_apply`), and the separate fact that for the unit `P ⟶ (sheaf P).presheaf`
that stalk map is an isomorphism (what the statement inverts).

**Finding 10 — done, with a correction to the review's parenthetical** (`59072cca`).
`OpenRestriction.lean` module docstring. The review says finite limits come "from
`restriction_rightAdjoint`, i.e. from `j^* ⊣ j_*`"; the first half is right and the parenthetical is
not. `restriction_rightAdjoint`'s witness is
`sheafPullbackConstruction.sheafAdjunctionContinuous (openImage U)`, i.e. `j_! ⊣ j^*`, and a right
adjoint preserves limits; `j^* ⊣ j_*` is `restriction_leftAdjoint`, which gives the *colimits*. The
docstring therefore now distributes all three facts: finite limits from `j_! ⊣ j^*`, finite colimits
from `j^* ⊣ j_*`, injectives from `j_!` preserving monomorphisms (`restriction_preservesInjectiveObjects`
is built from the same `sheafAdjunctionContinuous`). "Godement II.4" in the same sentence became
"Godement II §4" (item unverified).

**Finding 11 — done** (`41a8aa9a`). `DegreeZeroAcyclic.lean` title is now "Acyclicity of the
degree-zero singular-cochain sheaf in degree one", and the module docstring names
`zeroCochainSheaf_h1_subsingleton` as the only degree proved. `ComparisonPositive.lean` module
docstring no longer says the space is paracompact: it says the textbook hypothesis is
paracompactness while the declarations assume the stronger `[MetrizableSpace X]`, which is what
`constantSheafGlobalIso`, `globalCochainComparison_homology_isIso_succ_of_metrizable`,
`constantSheafCohomologyIsoSingular`, `constantSheafCohomologyIsoSingular_global` and
`constantSheafCohomologyIsoSingular_naturality_of_global` take.

## Not in scope

Findings 6, 7 and 12 of `r7-packet-09.md` are receipt corrections (the missing per-packet
`envdiff.json`, the incomplete "forced by X" lists, the `GlobalPatch.lean` pin count 317) and belong
to the agent that owns the round-7 receipts; nothing under `Lib/reports/round-7/` or `Lib/reviews/`
was touched here.

## Build

One `lake build` over the thirteen edited modules (Lean v4.33.0, from the worktree root):

```
✔ Built Lib.Topology.Sheaves.SheafificationLocalGerm (4.9s)
✔ Built Lib.Topology.Sheaves.SingularCochainSheaf.GlobalPatch (6.1s)
✔ Built Lib.Topology.Sheaves.FiniteClosedPushforward.Exact (11s)
✔ Built Lib.Topology.Sheaves.SingularCochainSheaf.GlobalPatchLocal (7.1s)
✔ Built Lib.Topology.Sheaves.H1Vanishing.Flasque (16s)
✔ Built Lib.Topology.Sheaves.SingularCochainSheaf.DegreeZeroAcyclic (5.1s)
✔ Built Lib.Topology.Sheaves.OpenEmbeddingCohomology (24s)
✔ Built Lib.Topology.Sheaves.SingularCochainSheaf.ComparisonH1 (5.3s)
✔ Built Lib.Topology.Sheaves.OpenRestriction (30s)
✔ Built Lib.Topology.Sheaves.OpenRestriction.StalkCriterion (3.5s)
✔ Built Lib.Topology.Sheaves.FiniteClosedPushforward.Cohomology (24s)
✔ Built Lib.Topology.Sheaves.SingularCochainSheaf.ComparisonPositive (7.4s)
✔ Built Lib.Topology.Sheaves.FiniteClosedPushforward.Composition (14s)
Build completed successfully (2725 jobs).
```

`set_option warningAsError true` is on in most of these files, so the build also covers the
docstring linters. No environment diff: the diff of every commit is comment text only.

## Commits

| commit | files | findings |
|---|---|---|
| `03a708e3` | `OpenEmbeddingCohomology.lean` | 1 |
| `59072cca` | `OpenRestriction.lean` | 10 |
| `a0193456` | `FiniteClosedPushforward/Exact.lean` | 2, 3 |
| `6e1dd154` | `FiniteClosedPushforward/{Cohomology,Composition}.lean` | 3 |
| `be89316b` | `SingularCochainSheaf/{GlobalPatch,GlobalPatchLocal}.lean`, `H1Vanishing/Flasque.lean` | 4 |
| `abb32368` | `SingularCochainSheaf/ComparisonH1.lean` | 4, 8 |
| `41a8aa9a` | `SingularCochainSheaf/{ComparisonPositive,DegreeZeroAcyclic}.lean` | 4, 11 |
| `ed6f57fa` | `OpenRestriction/StalkCriterion.lean` | 5 |
| `6b5b74f3` | `SheafificationLocalGerm.lean` | 9 |

Three files carry two findings each because the Bredon/Godement citations of finding 4 sit in the
same files as findings 8 and 11; splitting them would have produced commits that do not build as
units of meaning. Each commit body names every finding it closes.
