# Fixes for `Lib/reports/review-7-8/r7-packet-02.md`

Worktree `/home/goblin/hopf-fix-p02`, branch `fix/p02`, base `62d45257` (`lib/integration`).
Scratch `/home/goblin/.claude/jobs/06995e68/tmp/fix-p02/`.

Assignment: findings 1 and 2 (code).  Findings 3-8 are corrections to the text of
`Lib/reports/round-7/packets/RECEIPT-02.md`, which this agent must not touch; they are left to
the agent that owns the receipt corrections.

## Finding 1 — Coproduct.lean: the finite-biproduct pin is a local fix

Done.  Commit `ec1e2f77`.

The review is right: `CategoryTheory.Limits.HasFiniteBiproducts.of_hasFiniteProducts` gets stuck
on a universe constraint at a bare `u`, but `CategoryTheory.Abelian.hasFiniteBiproducts`
elaborates at `ChainComplex (ModuleCat.{u} ℤ) ℕ` (the abelian structure of the complex category
is found by `inferInstance`).  Both instance theorems were restated at `ModuleCat.{u} ℤ` with
that proof term, `universe u` was added, and the seven abstract pins were lifted with unchanged
proofs.

Lifted (9 `ModuleCat.{0}` occurrences → `ModuleCat.{u}`, 8 declarations):

* `Coproduct.singularChainsFiniteBiproducts` (new proof term)
* `Coproduct.homologyFiniteBiproducts` (new proof term)
* `Coproduct.homology_π_ι_self`
* `Coproduct.homology_π_ι_ne`
* `Coproduct.homology_biproduct_total`
* `Coproduct.homologyBiproductEquiv`
* `Coproduct.homologyBiproductEquiv_symm_apply`
* `Coproduct.homologyBiproductEquiv_desc` (two occurrences: the `K` and the `L` pin)

The file now has no `ModuleCat.{0}` left.  The `sigma*` declarations were never pinned: they are
about `SingularChains.singularComplex (X i)` for `X : ι → Type`, so they sit at universe 0 by
construction.

The `u = 0` instance of each lifted declaration is literally the old statement; checked by
elaborating the old statements verbatim against the new declarations
(`$S/scratch/Chk2.lean`: `Coproduct.singularChainsFiniteBiproducts`,
`Coproduct.homologyFiniteBiproducts`, `@Coproduct.homology_π_ι_self`,
`@Coproduct.homologyBiproductEquiv` — all accepted; the only diagnostic is a
`dependsOnNoncomputable` compile note on the `homologyBiproductEquiv` `example`, which is not an
elaboration failure).

The review's open question ("I did not test whether the seven downstream declarations then go
through") is answered: they do, with no proof change.

## Finding 2 — PositivePrimitives / Vanishing: the coefficient pins are not forced

Done.  Commits `fdfd786b` (PositivePrimitives) and `302c89fa` (Vanishing).

The review is right: only `SingularCochains.chains` is pinned, and it pins the *chain* side.
`dualComplex`, `complex`, `pullback`, `pullbackHomotopy` and `homotopyEquivCohomologyIso` all
take `A : AddCommGrpCat.{w}` (`Lib/AlgebraicTopology/SingularCochains.lean` l.82/122/126/160/175).

**PositivePrimitives** — all seven `A : AddCommGrpCat.{0}` binders restated at
`AddCommGrpCat.{w}`, proofs unchanged: `dualHomotopyEquiv`, `pointCochainHomotopyEquiv`,
`dualPointSingle_exactAt`, `pointCochain_exactAt_positive`, `pointCocycle_boundary`,
`pullback_closed_succ`, `nullhomotopic_pullback_closed_succ`.  This includes the two private
helpers the review could not reach from a scratch file (`pointCochainHomotopyEquiv`,
`dualPointSingle_exactAt`); `pointChainHomotopyEquiv` has no coefficient binder at all.

**Vanishing** — all five `A : AddCommGrpCat.{0}` binders restated at `AddCommGrpCat.{w}`, proofs
unchanged: `cohomology_subsingleton_of_homotopyEquiv`,
`cohomology_subsingleton_iff_of_homotopyEquiv`, `cohomology_subsingleton_iff_of_homeomorph`,
`pointCohomology_subsingleton`, `contractibleCohomology_subsingleton`.  The review had tested
three of these; the remaining two (which go through `pointCochain_exactAt_positive`) lift as well
once that theorem is lifted.

**Left pinned, with the reason:**

* `PositivePrimitives.pointFreeModule : ModuleCat.{0} ℤ`, the
  `ChainComplex.single₀ (ModuleCat.{0} ℤ)` occurrences in `pointChainHomotopyEquiv`,
  `pointCochainHomotopyEquiv` and `dualPointSingle_exactAt`, and
  `dualHomotopyEquiv`'s `{K L : ChainComplex (ModuleCat.{0} ℤ) ℕ}`.  These are the chain side,
  which goes through `SingularCochains.chains X : ChainComplex (ModuleCat.{0} ℤ) ℕ`.  That is the
  chain universe, a different generalisation axis from the coefficient pin this finding is about,
  and it is not local to these files.
* The two `AddCommGrpCat.of (ULift.{0} ℤ)` coefficients in
  `Vanishing.uliftIntCohomology_subsingleton_of_projective_of_homology` and
  `.uliftIntCohomology_subsingleton_of_adjacent_homology`.  They go through
  `DualEvaluation.LocalUCT.uliftIntCohomologyEvaluation` and
  `singularUliftIntCohomologyEvaluation_isIso_of_projective`, stated for that one coefficient
  group, and through `chains X`.  Generalising them is a change to the local UCT interface.

**Docstrings.**  Both module docstrings that said "arbitrary coefficient group" now name the
binder and the universe and say what stays at universe 0.  The two
"arbitrary *small* abelian coefficients" docstrings on `pointCohomology_subsingleton` and
`contractibleCohomology_subsingleton` no longer say "small", since `A` is now at `.{w}`.

## Builds

Per-module, foreground:

```
lake build Lib.AlgebraicTopology.SingularHomology.Coproduct        -> Build completed successfully (8710 jobs)
lake build Lib.AlgebraicTopology.SingularCochains.PositivePrimitives -> Build completed successfully (2024 jobs)
lake build Lib.AlgebraicTopology.SingularCochains.Vanishing        -> Build completed successfully (2130 jobs)
```

Full set (nohup + polled):

```
lake build Lib                                 -> exit 0
lake build Solution S6Shortcuts S6 Challenge   -> exit 0 (9196 jobs)
lake build Lib.AxiomAudit                      -> exit 0; 3300 `depends on axioms` lines,
                                                  only propext / Classical.choice / Quot.sound,
                                                  0 occurrences of sorryAx
python3 scripts/lib_stock_census.py --check    -> exit 0; "ratchet PASS: 123 <= baseline 1648"
```

## Environment diff

`$S/dump_base.jsonl` (before the first edit) vs `$S/dump_after.jsonl`, both 38254 constants.
No renames, so no `--rename` file.

```
constants before 38254 after 38254 (keys 38152 38152)
lost 39 added 39 of which source declarations: 0 0
names with changed type 39 of which source: 30
VERDICT PASS
```

**0 lost source names, 0 added.**  All 30 changed source types are universe generalisations:

* 8 direct — the declarations listed under finding 1 (`ModuleCat.{0}` → `ModuleCat.{u}`).
* 12 direct — the 7 PositivePrimitives and 5 Vanishing declarations listed under finding 2
  (`AddCommGrpCat.{0}` → `AddCommGrpCat.{w}`).
* 10 downstream in Coproduct.lean: `sigmaChainComplexMap`, `sigmaChainComplexMap_inclusion`,
  `sigmaChainInverseDegree`, `sigmaChainInverseDegree_inclusion`,
  `sigmaChainInverseDegree_comp_inclusion`, `sigmaChainComplexInverse`,
  `sigmaChainComplexInverse_inclusion`, `sigmaChainComplexMap_comp_inverse`,
  `sigmaChainComplexInverse_comp_map`, `sigmaChainComplexIso`.  Their *surface* statements are
  unchanged (checked with `pp.universes`: every universe in them is still `0`).  What changed is
  the `[HasBiproduct f]` instance argument buried inside `CategoryTheory.Limits.biproduct`, which
  is now elaborated from `Coproduct.singularChainsFiniteBiproducts.{0}` (a universe-polymorphic
  constant with a new proof term) instead of the old monomorphic one.  `HasBiproduct` is a
  `Prop`-valued class (`Mathlib/CategoryTheory/Limits/Shapes/Biproducts.lean:328`), so the old and
  new types are definitionally equal by proof irrelevance.

The 39 non-source lost/added entries are the corresponding auto-generated auxiliaries
(`_proof_n`, `.eq_1`) of the same declarations; the tool does not judge them.

Artefacts: `$S/dump_base.jsonl`, `$S/dump_after.jsonl`, `$S/envdiff.json`, `$S/envdiff.txt`,
where `$S = /home/goblin/.claude/jobs/06995e68/tmp/fix-p02`.

## Commits

```
ec1e2f77 Lib/AlgebraicTopology/SingularHomology/Coproduct.lean: lift the finite-biproduct instances and the abstract biproduct pins to ModuleCat.{u}
fdfd786b Lib/AlgebraicTopology/SingularCochains/PositivePrimitives.lean: lift the coefficient group to AddCommGrpCat.{w}
302c89fa Lib/AlgebraicTopology/SingularCochains/Vanishing.lean: lift the coefficient group to AddCommGrpCat.{w}
```

plus this receipt.
