# Fixes for `Lib/reports/review-7-8/r7-packet-10.md`

Worktree `/home/goblin/hopf-fix-p10`, branch `fix/p10`, base `62d45257` (`lib/integration`).
Findings 1 and 6 are receipt/commit-message corrections and belong to the receipt-owning agent;
this branch fixes findings 2, 3, 4, 5 and 7.

## Per finding

**Finding 2 — `PrimitivesH1.lean` universe pin not forced.** Done, commit `69ffe4aa`.
`section Homotopy` in `Lib/Topology/Sheaves/SingularCochainSheaf/PrimitivesH1.lean` now reads

```
universe u

variable {K L : CochainComplex AddCommGrpCat.{u} ℕ} {f g : K ⟶ L}
```

scoping the two private lemmas `homotopy_apply_closed_one` and `homotopy_apply_closed_zero`.
Nothing else in the file changed; both lemmas are statements about an arbitrary `Homotopy f g` of
cochain complexes and mention no singular chains, so the `u = 0` instances are literally the old
statements and the two in-file call sites still use them at `.{0}`. Pins in the file: 14 → 13.

**Finding 3 — `Vanishing.lean` module docstring.** Done, commit `af49328b`. "On a locally
contractible *paracompact* space" → "On a locally contractible metrizable (hence paracompact)
space". Both theorems in the file, `constantSheafCohomology_subsingleton_iff_singular` and
`constantSheafCohomology_succ_subsingleton_of_contractible`, take `hLC : LocallyContractibleSpace
X` and `[MetrizableSpace X]` (the second also `[ContractibleSpace X]`); the new wording matches
them and `ResolutionPositive.lean`.

**Finding 4 — "hereditarily paracompact".** Done, commit `3f868d2c`. In
`OpenRestriction.lean` both the module docstring and the docstring of
`isFlasque_of_open_normal_paracompact` now say "a space all of whose open subspaces are normal and
paracompact (for instance a metrizable space)", which is exactly
`[∀ V : Opens X, NormalSpace V] [∀ V : Opens X, ParacompactSpace V]`; conclusion
`(sheaf X A n).IsFlasque` unchanged. The metrizable instance remains
`isFlasque_of_metrizable` in the same file.

**Finding 5 — stray blank lines inside four signatures.** Done, commit `68a894aa`. Removed in
`GlobalUnit.lean` (`globalCochainComparison_f`), `GlobalUnitPositive.lean`
(`globalCochainComparison_homology_isIso_succ`), `Presheaf.lean` (`complex_d`) and
`Pullback/Sheaf.lean` (`cochainPullbackComplex_f`). Whitespace only.

**Finding 7 — unverified citation item numbers.** Done, commit `ecb2210c`. No copy of Iversen,
Bredon or Warner was available here, so every item number that could not be verified was replaced
by a section reference or a "cf.":

* `Iversen, *Cohomology of Sheaves* II.1` → `cf. Iversen, *Cohomology of Sheaves*` (no chapter
  number: the review's reading is that the stalk/exactness material is Chapter I, and that could
  not be checked) in `SingularCochainSheaf/LocalKernels.lean`, `StalkwiseSectionLift.lean`,
  `StalkwiseSectionRange.lean`. The verified Hartshorne II Ex. 1.2 stays and now comes first.
* `Bredon, *Sheaf Theory* III Prop. 1.1` (`GlobalSections.lean`, module + `globalCochainUnit_
  surjective`) and `III Thm. 1.1` (`GlobalUnitPositive.lean`, module +
  `globalCochainComparison_homology_isIso_succ`; `Vanishing.lean` module) → `Bredon, *Sheaf
  Theory* III §1`.
* `Warner … 5.32` cited for flasqueness of `𝒮^n(·; A)` → `Bredon, *Sheaf Theory* III §1` in
  `resolution_isAcyclic` (`ResolutionPositive.lean`) and in the `OpenRestriction.lean` module
  docstring (Warner Ch. 5 argues with *fine* sheaves). `ResolutionPositive.lean`'s module
  docstring keeps Warner 5.31 for the resolution itself (`5.31–5.32` → `5.31`).
* Left untouched: `Warner … 5.32` in `Pullback/FiniteClosedPositive.lean` and
  `Pullback/ComparisonH1.lean`, where it is cited for the comparison theorem, not for
  flasqueness; the `Bredon III Prop./Thm. 1.1` occurrences outside this packet's 24 files
  (`GlobalPatch.lean`, `GlobalUnitPredicates.lean`, `ComparisonH1.lean`,
  `ComparisonPositive.lean`) belong to other packets.

Findings 1 and 6: not this branch (receipt text under `Lib/reports/round-7/`).

## Builds

```
lake build Lib                              → Build completed successfully (9144 jobs).
lake build Solution S6Shortcuts S6 Challenge → Build completed successfully (9196 jobs).
lake build Lib.AxiomAudit                   → Build completed successfully (9144 jobs);
                                              axiom sets only [propext, Classical.choice,
                                              Quot.sound] / [propext, Quot.sound] / [propext];
                                              no sorryAx
python3 scripts/lib_stock_census.py --check → ratchet PASS: 123 <= baseline 1648
```

(`Challenge.lean:42` still warns `declaration uses 'sorry'`, as at the base; no file of this
branch touches it.)

Per-module builds before each commit: `PrimitivesH1`, `Vanishing`, `OpenRestriction`,
`GlobalUnit`+`GlobalUnitPositive`+`Presheaf`+`Pullback.Sheaf`, and
`GlobalSections`+`GlobalUnitPositive`+`LocalKernels`+`OpenRestriction`+`ResolutionPositive`+
`Vanishing`+`StalkwiseSectionLift`+`StalkwiseSectionRange` — all green.

## Environment diff

`lean-agent-ide dump Solution Lib --modules Hopf,Lib` before the first edit and after the last,
then `envdiff.py` (no renames, no `--rename`):

```
constants before 38254 after 38254 (keys 38152 38152 )
lost 2 added 2 of which source declarations: 0 0 ; names with changed type 2 of which source: 2
  PROOF-NAMING TopCat.SingularCochainSheaf.homotopy_apply_closed_one
  PROOF-NAMING TopCat.SingularCochainSheaf.homotopy_apply_closed_zero
auxiliary lost/added/changed (not judged): 2 2 0
module moves (source declarations, 1-to-1):
ambiguous module changes: 0
auxiliary constants that changed module: 0
VERDICT PASS
```

0 source names lost, 0 added; the two changed types are exactly the two private lemmas of
finding 2 (`.{0}` → `.{u}`, same statement at `u = 0`), and the 2 lost/2 added auxiliaries are
their own proof auxiliaries under the same names. Every other edit is docstring or whitespace and
leaves the environment unchanged.

## Commits

`62d45257..ecb2210c` (5 commits + this receipt):

* `69ffe4aa` PrimitivesH1.lean: lift the cochain-homotopy section to `AddCommGrpCat.{u}` (f2)
* `af49328b` Vanishing.lean: state the metrizability hypothesis in the module docstring (f3)
* `3f868d2c` OpenRestriction.lean: drop the non-standard "hereditarily paracompact" (f4)
* `68a894aa` SingularCochainSheaf: remove four stray blank lines inside signatures (f5)
* `ecb2210c` Lib/Topology/Sheaves: replace unverified citation item numbers by section
  references (f7)
