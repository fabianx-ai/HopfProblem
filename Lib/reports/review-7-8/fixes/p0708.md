# Fix receipt — p0708 (r7-packet-07, r7-packet-08)

Worktree `/home/goblin/hopf-fix-p0708`, branch `fix/p0708`, base `62d45257` (`lib/integration`).
Docstring, comment and citation edits only: no statement, hypothesis, binder, universe, name or
attribute was changed, nothing was added, deleted or renamed, no `sorry`/`axiom`/`admit`.  No
file under `Lib/reports/round-7/` or `Lib/reports/round-8/` was touched.  Per the brief, a
docstring-only agent skips the environment dump; there is no envdiff for this branch (and none
is needed: `git diff --stat 62d45257..HEAD` touches only comment text).

## r7-packet-07

### Finding 1 — manuscript labels surviving in declaration docstrings — **done**

Three named docstrings rewritten as mathematical statements (hypotheses listed first, then the
conclusion), the `receipts` jargon replaced, and the whole of `Lib/` swept for the three
patterns.

| site | was | now |
|---|---|---|
| `Lib/Topology/Sheaves/Cohomology/Cech/CochainSheafResolution.lean` `insertionHomotopy` | "This is equation (C13), transported through the canonical comparison…" | the degree-lowering operator from sections of `cochainSheaf F U (n+1)` over `W` to sections of `cochainSheaf F U n` over `W`, given by prepending the index `i`; it is `terminalInsertion` for the restricted family `fun j => W ⊓ U j` (whose member at `i` contains the others because `W ≤ U i`), transported through `cochainSheafSectionsIso` |
| same file, `insertionHomotopy_contraction` | "This is the normalized form of equation (C14)." | with `h k := insertionHomotopy … k` (degree `k+1 → k`) and `d k := cochainSheafDifferential F U k` (degree `k → k+1`), the identity `h n ≫ d n + d (n+1) ≫ h (n+1) = 𝟙` on sections of `cochainSheaf F U (n+1)` over `W` |
| `Lib/Topology/Sheaves/Cohomology/Cech/DegreeZero.lean` `cechCohomologyZeroIsoGlobalSections` | "This is equation (C8), valid without separation or paracompactness hypotheses." | for an abelian sheaf `F` on an arbitrary space `X`, refinement-colimit degree-zero Cech cohomology is canonically isomorphic to `F(⊤)`; no separation or paracompactness hypothesis is used |
| `Lib/Topology/Dimension/CubeBoundaryThreeDimension.lean` `finiteBrickRefinementOfScale` | "assemble the previously proved receipts at one supplied scale." | what the definition packages at the supplied mesh: the brick family at `h = 2 / N` with margin `epsilon`, its finite index type, the open-cover property, the refinement of `U`, and the multiplicity bound `3` |

Sweep of the whole of `Lib/` (`grep -rn '(C[0-9]\+)' / 'CD-[0-9]' / -i 'receipt' Lib/ --include=*.lean`):

* `(C[0-9]+)` — 9 hits at base.  Besides the four above and `LongExact.lean:47` (packet 08
  finding 4, below), two further survivors were found and fixed:
  * `Lib/Topology/Sheaves/Cohomology/Cech/Effacement.lean` — all three declaration docstrings
    opened "Textbook source: … (C28)".  Each now states the hypotheses and conclusion of its
    declaration (`cechCohomology_isZero_godementEnvelope`,
    `cechCohomology_germEmbedding_eq_zero`, `cechCohomologyDeltaFunctor_effaceable`).  No
    citation was substituted: the manuscript pointer identified no textbook item, and I would
    have had to invent one.
  * `Lib/Topology/Sheaves/Cohomology/Cech/DerivedGlobalSections.lean:160` — the section
    docstring above `cechToDerivedGlobalSections` ended "(textbook M14, lines 1880–1891 through
    'sections must be (C30).')"; the pointer is dropped.
* `CD-[0-9]` — 0 hits at base; nothing to do.
* `receipt` — 7 hits at base.  Two were manuscript jargon and are fixed: the
  `CubeBoundaryThreeDimension.lean:261` comment above, and
  `Cech/DerivedGlobalSections.lean:123` ("the fixed α-zero and Φ-zero receipts cancel the SAME ε
  inverse and ε"), whose docstring now states its theorem (in degree zero the comparison is the
  degree-zero Cech-to-global identification followed by the inverse of the degree-zero
  derived-global identification).  The remaining five are **left deliberately**: they are
  cross-references to real provenance files in this repository, not manuscript labels —
  `Lib/Geometry/Manifold/Morse/OrderedCancellation.lean:67`,
  `Lib/Geometry/Manifold/Morse/SurgeryCollapse.lean:79`,
  `Lib/AlgebraicTopology/SingularHomology/OnePointCover.lean:67`,
  `Lib/AlgebraicTopology/SingularHomology/LocalDegreeNeighborhoods.lean:63` (all pointing at
  `Lib/reports/integration-4/spheretop-moves.md`, which exists) and
  `Lib/Topology/Sheaves/Cohomology/AddCommGroup.lean:36` (pointing at
  `Lib/reports/round-7/names/RECEIPT.md`, which exists).

After the fixes the three sweep greps return: `(C[0-9]+)` 0 hits, `CD-[0-9]` 0 hits, `receipt`
only the five in-repo cross-references listed above.

**Left, out of scope:** `Cech/DerivedGlobalSections.lean` still carries manuscript jargon that
matches none of the three assigned patterns — "textbook M13", "C29i" and a bare `C30` in about
fifteen further docstrings (lines 14–15, 40, 47, 54, 64, 76, 100, 136–137, 152, 156, 164, 229,
247, 264).  Repo-wide, `grep -rn 'textbook\|Textbook' Lib/ --include=*.lean` has 66 hits in 27
files.  That is a separate sweep and was not attempted here.

### Finding 2 — Hatcher "Example 1.22" — **done**

`Lib/Topology/Homotopy/PuncturedPlaneCyclic.lean`, module docstring.  Theorem 1.7 is kept for
`π₁(S¹) ≅ ℤ`.  The Example 1.22 number could not be verified, so per the brief the deformation
retraction of `ℂ ∖ {0}` onto `S¹` is cited at section level instead:

```
* A. Hatcher, *Algebraic Topology*, Theorem 1.7 (`π₁(S¹) ≅ ℤ`)
* A. Hatcher, *Algebraic Topology*, Chapter 0 and §1.1 (the deformation retraction of
  `ℂ ∖ {0}` onto `S¹`)
```

### Finding 3 — Weibel "Theorem 2.4.6" — **done, as a section reference**

`Lib/Topology/Sheaves/Cohomology/AcyclicResolution.lean:24` and
`Lib/Topology/Sheaves/Cohomology/AcyclicResolutionH1.lean:28`.  I could not verify either
"Theorem 2.4.6" or the suggested "Exercise 2.4.3" against the book, so under the brief's rule
("a citation you cannot verify is left as a section reference") both now read

```
* C. Weibel, *An Introduction to Homological Algebra*, §2.4 (cf. §2.5 for the right-derived
  case)
```

Hartshorne III, Proposition 1.2A, correct for this statement, is kept unchanged in both files.
Note for the owner: `Lib/Algebra/Homology/DerivedCategory/Ext/AcyclicResolutionH1.lean:22`,
`…/AcyclicResolutionH2H3.lean:20` and `…/AcyclicResolutionH1ExactFunctor.lean:20` cite Weibel
"Theorem 2.4.3" for the same fact; those files are outside this assignment and were not touched,
but the two numberings cannot both be right.

### Finding 8 — `isAcyclicFor` docstring reads as an iff — **done**

`Lib/Topology/Sheaves/Cohomology/AcyclicResolution.lean`.  The theorem is the single implication
`IsAcyclic R → R.IsAcyclicFor (TopCat.ConstantSheaf.integralSheaf X)`, proved by the hypothesis
itself.  The docstring now states that implication and records that the two properties unfold to
the same statement (which is why `h` is a proof), instead of "is *exactly* acyclicity".

### Findings 4, 5, 6, 7 — not mine

Receipt-arithmetic and missing-envdiff findings; owned by the receipt-correction agent.

## r7-packet-08

### Finding 4 — the surviving `(C24)` — **done**

`Lib/Topology/Sheaves/Cohomology/Cech/LongExact.lean:47`,
`initialCoefficientMap_injective`.  The docstring now states the declaration: for a short exact
sequence `S` of abelian sheaves on `X`, the degree-zero Cech coefficient map induced by the left
map `S.f` is injective; it is the initial injection of the long exact sequence, obtained from
injectivity of `S.f` on global sections through the canonical degree-zero identification.

### Finding 5 — `basedFibreInclusion_isClosedMap` hypotheses — **done**

`Lib/Topology/Sheaves/ConstantProductH1.lean`.  The declaration is under `omit [CompactSpace S]
in`, so its hypotheses are `[T2Space S] [ContractibleSpace S] [CompactSpace X] [T2Space X]`, and
the proof is `(basedFibreInclusion S X s).hom.continuous.isClosedMap` (compact source, Hausdorff
target).  The docstring no longer claims the product is compact Hausdorff; it says the map is
continuous, the source `X` is compact, the target `S × X` is Hausdorff because both factors are,
and compactness of `S` is not used.

### Finding 6 — Hartshorne "III Ex. 8.1" — **done, dropped**

`Lib/Topology/Sheaves/FiniteClosedPushforward/AcyclicResolution.lean`.  III Ex. 8.1 is the
sheafification description of `R^i f_*` and is not the stated general fact; the suggested
III Ex. 8.2 (flasque sheaves are `f_*`-acyclic) is not that fact either, so rather than swap one
unverified item for another the exercise number is dropped and III.8 is cited at section level:

```
… see Hartshorne, *Algebraic Geometry*, III.1.2A (acyclic
resolutions compute derived functors); cf. III.8 for the higher direct images of a pushforward.
```

Hartshorne III.1.2A, correct for the statement, is kept.

### Finding 8 — Iversen "III.2" and Godement "II.5.10.1" — **done, softened**

* `Lib/Topology/Sheaves/ConstantProductH1Comparison.lean`: "and Iversen, *Cohomology of
  Sheaves*, III.2" → "and cf. Iversen, *Cohomology of Sheaves*, III".  Bredon II.11.12 is kept.
* `Lib/Topology/Sheaves/ConstantSheafH1.lean`: "Bredon, *Sheaf Theory*, III.1.1, and Godement …
  II.5.10.1 (sheaf cohomology with constant coefficients agrees with singular cohomology on a
  paracompact locally contractible space)" → the statement text now sits with Bredon III.1.1,
  which is correct for it, and Godement is cited as "cf. … II.5.10" (the number already used for
  section-level Godement references elsewhere in `Lib`).

### Findings 1, 2, 3, 7 — not mine

Forcing-claim, arithmetic, missing-envdiff and envdiff-table findings; owned by the
receipt-correction agent.

## Build

One `lake build` over all thirteen edited modules, from the worktree root with the pinned lake
(`/home/goblin/.elan/toolchains/leanprover--lean4---v4.33.0/bin/lake`, `GIT_CONFIG_*` set):

```
lake build Lib.Topology.Dimension.CubeBoundaryThreeDimension \
  Lib.Topology.Homotopy.PuncturedPlaneCyclic \
  Lib.Topology.Sheaves.Cohomology.AcyclicResolution \
  Lib.Topology.Sheaves.Cohomology.AcyclicResolutionH1 \
  Lib.Topology.Sheaves.Cohomology.Cech.CochainSheafResolution \
  Lib.Topology.Sheaves.Cohomology.Cech.DegreeZero \
  Lib.Topology.Sheaves.Cohomology.Cech.DerivedGlobalSections \
  Lib.Topology.Sheaves.Cohomology.Cech.Effacement \
  Lib.Topology.Sheaves.Cohomology.Cech.LongExact \
  Lib.Topology.Sheaves.ConstantProductH1 \
  Lib.Topology.Sheaves.ConstantProductH1Comparison \
  Lib.Topology.Sheaves.ConstantSheafH1 \
  Lib.Topology.Sheaves.FiniteClosedPushforward.AcyclicResolution
✔ [3247/3276] Built Lib.Topology.Dimension.CubeBoundaryThreeDimension (14s)
✔ [3248/3276] Built Lib.Topology.Homotopy.PuncturedPlaneCyclic (15s)
✔ [3249/3276] Built Lib.Topology.Sheaves.ConstantProductH1 (16s)
✔ [3250/3276] Built Lib.Topology.Sheaves.Cohomology.AcyclicResolutionH1 (25s)
✔ [3265/3276] Built Lib.Topology.Sheaves.ConstantSheafH1 (7.0s)
✔ [3266/3276] Built Lib.Topology.Sheaves.ConstantProductH1Comparison (5.3s)
✔ [3267/3276] Built Lib.Topology.Sheaves.Cohomology.Cech.CochainSheafResolution (57s)
✔ [3269/3276] Built Lib.Topology.Sheaves.Cohomology.AcyclicResolution (35s)
✔ [3270/3276] Built Lib.Topology.Sheaves.FiniteClosedPushforward.AcyclicResolution (28s)
✔ [3271/3276] Built Lib.Topology.Sheaves.Cohomology.Cech.DegreeZero (142s)
✔ [3272/3276] Built Lib.Topology.Sheaves.Cohomology.Cech.LongExact (66s)
✔ [3274/3276] Built Lib.Topology.Sheaves.Cohomology.Cech.Effacement (3.6s)
✔ [3276/3276] Built Lib.Topology.Sheaves.Cohomology.Cech.DerivedGlobalSections (22s)
Build completed successfully (3276 jobs).
done 0
```

`set_option warningAsError true` is on in every one of these files, so the build also certifies
that no docstring linter fires.

## Commits

`62d45257..` on `fix/p0708`, one per fix item, in order:

| commit | item |
|---|---|
| `e6ab6501` | `CochainSheafResolution.lean` — packet 07 finding 1 |
| `1f287d4e` | `Cech/DegreeZero.lean` — packet 07 finding 1 |
| `61fb7b31` | `CubeBoundaryThreeDimension.lean` — packet 07 finding 1 |
| `e9ea51ee` | `Cech/Effacement.lean` — packet 07 finding 1 sweep |
| `643ed344` | `Cech/DerivedGlobalSections.lean` — packet 07 finding 1 sweep |
| `e37de810` | `Cech/LongExact.lean` — packet 08 finding 4 |
| `e24c2aca` | `PuncturedPlaneCyclic.lean` — packet 07 finding 2 |
| `9a67d4ba` | `AcyclicResolution.lean`, `AcyclicResolutionH1.lean` Weibel — packet 07 finding 3 |
| `ae43b5fb` | `AcyclicResolution.lean` `isAcyclicFor` — packet 07 finding 8 |
| `b421966b` | `ConstantProductH1.lean` — packet 08 finding 5 |
| `fd78383d` | `FiniteClosedPushforward/AcyclicResolution.lean` — packet 08 finding 6 |
| `615dcad2` | `ConstantProductH1Comparison.lean`, `ConstantSheafH1.lean` — packet 08 finding 8 |

plus this receipt.
