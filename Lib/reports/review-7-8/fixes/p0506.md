# Fix receipt p0506 — r7-packet-05 and r7-packet-06

Branch `fix/p0506`, base `62d45257` (`lib/integration`), worktree `/home/goblin/hopf-fix-p0506`.
Scope: the docstring and citation findings of `Lib/reports/review-7-8/r7-packet-05.md` and
`r7-packet-06.md` listed in the assignment. **Comment-only**: every edit is inside a `/--` or
`/-! -/` block; the comment-stripped sources of all twelve edited files are byte-identical to
their versions at `62d45257` (checked by a strip-and-compare script), so no statement, universe,
name or declaration changed and no environment diff was run (docstring-only agent).

## Findings

### r7-packet-05

1. **`WhitneyPairModel.cornerScale`** (`Lib/Geometry/Manifold/Whitney/BigonModel.lean:410`).
   Done. "interpolated distance to the **nearer** corner" → "to the **opposite** corner"; the rest
   of the sentence (`1 - t` near the left corner, `t` near the right one) was already correct and
   is kept. Commit `b190de25`.
2. **`CleanBigonBoundary` and the CleanStrips module bullet** (`CleanStrips.lean`). Done. Both said
   the neighbourhood "meets the two sheets exactly in the two edges of the bigon". The fields
   `interior_avoids` (`domain ∩ interior (bigon h)`) and `clean` (`bigon h ∩ closed_neighborhood`
   off `frontier (bigon h)`) constrain only the bigon side, so both now say the map avoids `S` and
   `T` at the points of the bigon off its frontier and state explicitly that nothing is asserted
   about the part of the neighbourhood outside the bigon. Commit `af11d3a2`.
3. **Milnor TDV §7** (`AnnularExtension.lean` module docstring). Done. Replaced by §§2–3
   (Sard–Brown, the argument actually formalised: a smooth representative of dimension `m < n` is
   not surjective); the Hatcher §4.1 pointer is kept and annotated "by cellular approximation". No
   item number invented. Commit `a6fc984f`.
4. **`realizes_det_one` / `realizes_local_germ` dimension ≥ 2** (`Transversality/Basic.lean:1808`,
   `:2206`, and the module-docstring bullet). Done: all three now say the space is of dimension at
   least two and point at the `[Finite ι] [Nontrivial ι] (b : Module.Basis ι ℝ E)` binders.
   Commit `9cbac3f8`.
5. **`CleanStripPatch`** (`CleanStrips.lean:2055`). Done: the docstring now says the patch agrees
   near `(0, 0)` with `k₀` and near `(1, 0)` with `k₁ ∘ StripCoordinates.reverse` (field
   `right_germ`), and that it meets `T` exactly in the two vertical lines `t = 0` and `t = 1`
   (field `second_sheet`), not in two points. Commit `af11d3a2`.
6. **`DiskShrinking.exists_chart_disk_shrinking`** (`Transversality/Basic.lean:2954`). Done:
   "supported near the image of the disc" → supported in a compact `K ⊆ Φ.target` inside the
   chart, fixing the centre `Φ (0, 0)`. Commit `9cbac3f8`.
7. **AnnularExtension module bullets.** Done: the `sphereMap_nullhomotopic_of_dim_lt` bullet names
   `[I.Boundaryless]`; the `exists_continuous_annular_extension` bullet now says the two boundary
   restrictions are asked to *extend over the closed unit ball* (the same thing as being
   nullhomotopic, via `SphereCone.extension` — the step is named rather than silently used) and
   that the extension is constant outside radius `2 b`. Commit `a6fc984f`.

Findings 8 of packet 05 (no per-packet envdiff artifact in the repo) is a receipt/tooling item and
was not in this assignment.

### r7-packet-06

3. **Four self-repeating docstrings.** Done, second sentence deleted in each:
   `FrameField.lean` `TubularBigon.rankThree_corner_sheet_charts_coincide` (l. 1865) and
   `StripNormalData.bijective_tubularTransitionDerivative` (l. 2233), commit `2d038916`;
   `RankThreeModel.lean` `StripNormalData.retimedDomain_contains_center` (l. 1051) and
   `TubularBigon.RankThreeTangentAdaptedChart.shearedCoordinates` (l. 1358), commit `39f81021`.
4. **`WhitneyPairModel.isCompact_innerBigonCollar`** (`EmbeddedArcs.lean:496`). Done: "For `0 < r`"
   → "For `0 < h` and `r ≠ 0`", the actual hypotheses `(hh : 0 < h) (hr : r ≠ 0)`. Commit
   `7307291e`.
5. **`ManifoldMorse.MorseSurgeryData.nonempty_belt_tubularBigon`** (`EmbeddedArcs.lean:3155`).
   Done: "embedded two-sphere" → "smooth two-sphere" with an explicit note that no injectivity or
   immersion hypothesis is assumed; `[CompactSpace M]` and the null-homotopy hypothesis `hnull`
   (every circle in `d.LowerLevel` is null-homotopic) are now named. Commit `7307291e`.
6. **`freeGroupGeneratorCokernelEquivGroupHomologyH0`.** Done: the docstring now says the single
   universe is forced by `groupHomology`, declared under `{k G : Type u} (A : Rep.{u} k G)`, and
   notes that `Rep` itself is polymorphic in all three universes. The statement is unchanged.
   Commit `5e11dcbe`.
7. **Citations.** Done.
   * (i) Brown "Ch. II" for the low-degree LHS sequence → **Ch. VII §6, Cor. VII.6.4** in
     `Lib/GroupTheory/Abelianization/SemidirectProduct.lean` and
     `Lib/GroupTheory/GroupExtension/Abelianization.lean`. Commit `091aafca`.
   * (ii) Brown "II.3" → **II.2** (coinvariants) and **III.1** (homology with coefficients,
     `H₀(G; M) = M_G`) in `Lib/RepresentationTheory/FreeGroupCoinvariants.lean` (commit
     `4219def5`); → **Ch. I §4** (the beginning of the free resolution of `ℤ` over `ℤF` for a free
     group), **II.2** and **III.1** in `Lib/RepresentationTheory/FreeGroupGeneratorCokernel.lean`
     (commit `5e11dcbe`). Only sections are cited: the example number inside Ch. I §4 is not one I
     can verify, so it is deliberately omitted.
   * (iii) Bourbaki **A III §8.5 → §7.8** (multiplication in the exterior algebra of a free module
     on a basis, `e_H e_K = ± e_{H ∪ K}`) in
     `Lib/LinearAlgebra/ExteriorPower/ExteriorProductCoordinates.lean`; §8.5–8.6 in
     `MatrixCoordinates.lean` (minors) is left untouched, as instructed. Commit `38c3e036`.
8. **(d) "no Mathlib counterpart" note.** Done: the one-line twin note dropped by the round-7
   rewrite is restored, in the form already used by `BigonModel.lean`, `CleanStrips.lean` and
   `AnnularExtension.lean`:

   ```
   ## Twin

   No Mathlib counterpart exists.
   ```

   in the module docstrings of `EmbeddedArcs.lean` (`7307291e`), `FrameField.lean` (`2d038916`)
   and `RankThreeModel.lean` (`39f81021`).

Findings 1, 2, 8a–c and 9 of packet 06 are receipt/tooling items (double-counted work items,
docstring totals, missing envdiff artifacts, dangling `.bib` keys) and were not in this
assignment; no file under `Lib/reports/` or `Lib/reviews/` other than this receipt was touched.

## Builds

`lake build <Module>` (Lean v4.33.0, lake from the v4.33.0 toolchain), one per edited module, all
exit 0, no error and no new warning (the only warning in the logs is the pre-existing
`Lib/AlgebraicTopology/SingularHomology/LocalContributions.lean:202:37` unused-binder hint in an
untouched file):

| module | exit |
|---|---|
| `Lib.Geometry.Manifold.Whitney.BigonModel` | 0 |
| `Lib.Geometry.Manifold.Whitney.CleanStrips` | 0 |
| `Lib.Geometry.Manifold.Whitney.AnnularExtension` | 0 |
| `Lib.Geometry.Manifold.Transversality.Basic` | 0 |
| `Lib.Geometry.Manifold.Whitney.EmbeddedArcs` | 0 |
| `Lib.Geometry.Manifold.Whitney.FrameField` | 0 |
| `Lib.Geometry.Manifold.Whitney.RankThreeModel` | 0 |
| `Lib.GroupTheory.Abelianization.SemidirectProduct` | 0 |
| `Lib.GroupTheory.GroupExtension.Abelianization` | 0 |
| `Lib.LinearAlgebra.ExteriorPower.ExteriorProductCoordinates` | 0 |
| `Lib.RepresentationTheory.FreeGroupCoinvariants` | 0 |
| `Lib.RepresentationTheory.FreeGroupGeneratorCokernel` | 0 |

## Environment diff

None. The edits are comment-only (comment-stripped sources identical to `62d45257` for all twelve
files), so no dump was taken and `$S/rename.txt` is empty: no name was added, lost or renamed and
no type changed.

## Commits

`62d45257..` on `fix/p0506`, one per fix item, all with the `Co-Authored-By` and `Claude-Session`
trailers:

| commit | item |
|---|---|
| `b190de25` | BigonModel: `cornerScale` (p05 f1) |
| `af11d3a2` | CleanStrips: `CleanBigonBoundary`, module bullet, `CleanStripPatch` (p05 f2, f5) |
| `a6fc984f` | AnnularExtension: module bullets and Milnor §§2–3 (p05 f3, f7) |
| `9cbac3f8` | Transversality/Basic: dimension ≥ 2 and `K ⊆ Φ.target` (p05 f4, f6) |
| `7307291e` | EmbeddedArcs: two docstrings, twin note (p06 f4, f5, f8d) |
| `2d038916` | FrameField: repeated sentences, twin note (p06 f3, f8d) |
| `39f81021` | RankThreeModel: repeated sentences, twin note (p06 f3, f8d) |
| `091aafca` | GroupTheory: Brown Ch. VII §6 (p06 f7 i) |
| `4219def5` | FreeGroupCoinvariants: Brown II.2 / III.1 (p06 f7 ii) |
| `5e11dcbe` | FreeGroupGeneratorCokernel: Brown Ch. I §4 / II.2 / III.1, `groupHomology` (p06 f6, f7 ii) |
| `38c3e036` | ExteriorProductCoordinates: Bourbaki A III §7.8 (p06 f7 iii) |
